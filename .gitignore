# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
.pytest_cache/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# Environment
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Temporary
*.tmp
*.bak

# Jetro / other tool state (may contain credentials)
.jetro/

# Claude Code conversation history — never commit (private + too large)
*.jsonl

# .claude-context/ is intentionally NOT gitignored — it's the sync feature
# Only exclude the conversation history files inside it if they ever appear:
.claude-context/*.jsonl
.claude-context/*.json
!.claude-context/sync-manifest.json
